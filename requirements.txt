pandas==3.0.2
numpy==2.4.4
scikit-learn==1.8.0
streamlit>=1.30
plotly>=5.18
joblib>=1.3

# NOTE ON VERSION PINNING:
# pandas, numpy, and scikit-learn are pinned to EXACT versions because the
# trained model files in models/*.pkl were serialized with these specific
# versions. Scikit-learn pickles are not guaranteed to be forward/backward
# compatible across versions -- loading a .pkl saved with one sklearn version
# using a different installed version can silently produce wrong results or
# raise an error. If you upgrade scikit-learn, re-run `python src/train_model.py`
# to regenerate the .pkl files rather than relying on the committed ones.
